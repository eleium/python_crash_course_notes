"""
✅ 集合、字典、列表的对比总表
维度	               列表（list）	                     字典（dict）	                                 集合（set）
符号	               []	                              {} + 冒号	                                     {}
元素类型	           任意元素	                          键值对（key: value）	                         只有值
是否有序	           ✅ 有序（按插入顺序）	            ✅ 有序（Python 3.7+）	                    ❌ 无序
是否可重复	           ✅ 元素可重复	                    ❌ 键不可重复（值可重复）	                 ❌ 元素不可重复
是否支持索引	       ✅ 支持（list[0]）	            ❌ 不支持	                                 ❌ 不支持
访问方式	            通过索引	                      通过键（dict["key"]）                        	不能通过索引/键访问
是否可变	            ✅ 可变	                        ✅ 可变	                                     ✅ 可变
空对象	                 []                            	  {}	                                         set()
常见方法	          .append(), .pop(), .sort()	      .keys(), .values(), .items(), .get()	       .add(), .remove(), .union()
🧪 代码示例对比

# 1. 列表
lst = [1, 2, 3, 2]
lst.append(4)          # [1, 2, 3, 2, 4]
print(lst[0])          # 1（支持索引）

# 2. 字典
d = {"a": 1, "b": 2}
d["c"] = 3             # 添加键值对
print(d["a"])          # 1（通过键访问）

# 3. 集合
s = {1, 2, 3, 2}       # 自动去重 → {1, 2, 3}
s.add(4)               # {1, 2, 3, 4}
# print(s[0])          # ❌ 不支持索引

📌 选择指南
场景	                                    推荐使用
需要保持顺序，元素可重复，需要索引访问	    列表（list）
需要通过键快速查找值，需要存储键值对	    字典（dict）
需要去重，不需要顺序，不需要索引	        集合（set）

📌 一句话总结
列表是有序、可重复、可索引的序列（[]）；
字典是有序、键唯一、值可重复的键值对容器（{} + 冒号）；
集合是无序、不可重复、不可索引的纯值容器（{}，空集合用 set()）。
"""